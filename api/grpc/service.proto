syntax = "proto3";

package consumer;

option go_package = "./api/grpc";

import "api/grpc/queue/service.proto";

service Service {
  rpc SubmitBatch(queue.SubmitMessageBatchRequest) returns (queue.BatchResponse);
}
